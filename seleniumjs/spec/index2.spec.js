const JASMINE = require('jasmine');

describe("A suite", () => {
    it("contains spec with an expectation", () => {
        expect(true).toBe(true);
    });
    it("contains spec with an expectation", () => {
        expect(true).toBe(true);
    });
});